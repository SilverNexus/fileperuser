-------------------------------------
FilePeruser Installation Instructions
-------------------------------------

1. Verify you meet the requirements in the README.
2. Follow these steps:
    a. cd to program directory (an "open terminal here" tool works, too)
    b. run autogen.sh ("sh autogen.sh" should work)
    c. run configure ("./configure" should work)
    d. run make ("make" and/or "sudo make install" should work)
    e. use the program
